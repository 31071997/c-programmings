#include <stdio.h>
#include <math.h>
int main(){
    int t,T, d, v, u;
    scanf("%d", &T);
    while(1)
    {
        for( t=1; t<= T; t++)
        {
            scanf("%d %d %d", &d, &v, &u);
            printf("Case %d:", t);
        }
    }
    return 0;
}
